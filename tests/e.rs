#![allow(dead_code, unused_imports)]

// Euler problem tests — commented out during syntax redesign.
// These will be rewritten in S-expression syntax when the new
// pipeline is stable.

use l::{
    ast::{E, NN},
    byte::I,
    vm::V,
};
